# Benchmarks

Schemas cover quality, detection, fusion, reconstruction, restoration, OCR/semantic outcomes, latency, memory, and battery/device class. `schema/mp2-scalar-baseline.example.json` shows the generic MP2 baseline envelope. See `docs/benchmark-format.md` for ScanBench policy and reproducibility expectations.
