# Specialist router

The router maps `ConditionVector` to `ProcessingPlan`, then orchestrates processors into `ProcessorResult` values. Processors will declare capabilities, expected benefit, estimated cost, supported device classes, confidence, and diagnostics.

