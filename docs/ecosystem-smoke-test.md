# Ecosystem smoke checklist

Run this repository’s Rust CI commands, run model registry/dataset/benchmark validation in `wellfriend-models`, and run the scanner web/contracts commands in `wellfriend-scan`. Confirm filter names, processor IDs, device classes, tasks, guidance, and exports against `ecosystem-compatibility.json`; confirm that mocks and blockers remain disclosed.
