# Development

Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, tests, docs, benchmark compilation, and both PowerShell registry/schema checks before proposing a change.
