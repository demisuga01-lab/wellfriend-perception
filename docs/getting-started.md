# Getting started

Clone this repository, install stable Rust, then run `cargo test --workspace --all-features`. Algorithms live here; models belong in `wellfriend-models`, while UI/capture belongs in `wellfriend-scan`. Do not add domain-specific product logic to generic core modules.
