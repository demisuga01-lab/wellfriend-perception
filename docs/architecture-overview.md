# Architecture overview

The engine flows from observation through quality, independent detection, fusion, refinement, temporal confidence, reconstruction, condition routing, restoration, semantics, and structured output. `DomainPack` composes domain extensions; document is the only substantial reference path.
