# Repository layout

`core` owns generic contracts; `image` owns safe baseline pixels. The remaining top-level module folders define the future implementation seams. `domains` plugs domain behavior into the core, `bindings` exposes stable adapters, `benchmarks` owns measurement schemas, and `third_party` owns dependency provenance.

