# Versioning

`0.1.0-alpha.1` means unstable APIs and technical-preview evidence. `implemented` is tested code; `experimental` is benchmark-limited; `mock` is dev-only; `placeholder` has no implementation; `audit-gated` cannot ship; `production_ready` is not used in this alpha.
