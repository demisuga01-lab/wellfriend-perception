# ADR-0001: Rust as the shared production core

Use Rust for deterministic, portable, memory-safe production contracts and processing. Platform adapters call Rust rather than duplicating algorithms.

