# Quality framework

Generic metrics are blur, noise, exposure, saturation, contrast, motion, glare, occlusion, and confidence. The document pack adds page coverage, page visibility, shadow, curvature, and residual perspective as domain metrics rather than core fields.

