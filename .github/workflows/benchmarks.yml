name: Scalar baseline benchmarks
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  scalar-baseline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo bench -p wellfriend-perception-image --bench baseline
