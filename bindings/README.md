# Bindings

The `c`, `cpp`, `python`, `android`, `wasm`, and `node` folders reserve adapter surfaces. Bindings must be thin, versioned, and map to the shared Rust core; they must not fork perception logic.

