# Detection framework

`classical`, `ml`, `contours`, `lines`, and `segmentation` each produce independently attributed `DetectionSet` values. Full ensembles are deferred; candidate contracts live in `core`.

