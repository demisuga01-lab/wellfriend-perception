# Reconstruction framework

Planar, surface, volumetric, geospatial, and photogrammetric strategies select a `GeometryModel` and return a `ReconstructionResult`. Documents will first use planar and surface implementations.

